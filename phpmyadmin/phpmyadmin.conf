ServerName localhost

# Disable server-status endpoint to avoid repeated 403 logs from probes
<IfModule mod_status.c>
    <Location /server-status>
        Require all denied
    </Location>
</IfModule>
